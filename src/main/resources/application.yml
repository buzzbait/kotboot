spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/buzzdb
    username: buzz
    password: buzz
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    database: postgresql
    hibernate:
      ddl-auto: update